QUESTÃO DISCURSIVA 3
K ũogo ^udoŬu consiste em uma matriz 9x9 dividida em 9 sub-matrizes 3x3,  como mostrado na fgura a seguir.
Disponível em фhtp:ͬͬ
ǁǁǁ. en-ǁiŬipedia. orgх.   cesso em:  26 ũul 2014 (adaptado).
  matriz estA parcialmente preenchida com numeros de 1 a 9,  e o obũetivo do ũogo é completar a matriz,  de 
forma que cada linha,  coluna e sub-matriz contenham todos os numeros de 1 a 9.
  partir dessas informações,  escreva um algoritmo recursivo baseado em retrocesso (backtracking) para 
resolver o ũogo.    matriz foi transformada em um vetor V de 81 posições,  contendo zeros nas posições que 
faltam para serem preenchidas.
Ionsidere  que  existem  duas  funções  implementadas.      primeira  função,   NaoHaViolacao(x,i,V),  
retorna verdadeiro se a inserção do numero x na posição i do vetor V não causa violação das restrições 
do ũogo (numero repetido em linha,  coluna ou sub-matriz).    segunda função, Imprime(V), realiza a 
impressão do vetor V.
Ionsidere,  ainda,  que o algoritmo deve imprimir o vetor V com a solução encontrada,  se esta existir.  
(valor:  10, 0 pontos)
Kbservação:  Yualquer notação em portugugs estruturado,  de forma imperativa ou orientada a obũetos pode 
ser utilizada,  assim como em uma linguagem de alto nível,  como Wascal,  I ou :ava.