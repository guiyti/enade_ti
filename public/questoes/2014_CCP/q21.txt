QUESTÃO 21
K fragmento de codigo a seguir,  escrito em :ava,  descreve duas implementações diferentes para um lock.
 mbas possuem um método denominado acquire e um método denominado release.
class LockA {
 private int turn = 0
 public void acquire(int tid) {
      while (turn == (1 - tid));              
 }
 public void release(int tid) {
 turn = (1 - tid);
 }
}
 class LockB {
 public void acquire() {
 disableInterrupts();
 }
 public void release() {
 enableInterrupts();
 }
 }
Ionsidera-se que:
ͻ  as duas implementações de lock são utilizadas por aplicações com,  no mAximo,  duas threads;
ͻ  uma aplicação que utilizar qualquer uma destas implementações invocarA o método acquire antes de 
entrar em sua seção crítica e o método release apos deixar a seção crítica͖
ͻ  tanto o método acquire quanto o método release são operações atƀmicas nas duas implementações 
de lock͖
ͻ  para a implementação que requer um tid (thread id),  assume-se que ele sempre serA 0 ou 1͖
ͻ  os métodos disableInterrupts e enableInterrupts são utilizados para desabilitar e habilitar 
respectivamente as interrupções do processador onde o codigo for executado.  K codigo desses dois 
métodos foi desenvolvido para ser utilizado em uma mAquina com um ou dois processadores.
  partir das informações apresentadas,  avalie as afrmações a seguir.
/.     implementação de LockA garante progresso.
//.     implementação de LockB garante progresso.
///.     implementação de LockA garante exclusão mutua.
/s.     implementação de LockB garante exclusão mutua.
 correto apenas o que se afrma em
A / e //.
B // e ///.
C /// e /s.
D /,  // e /s.
E /,  /// e /s.