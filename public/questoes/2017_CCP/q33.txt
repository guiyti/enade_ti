QUESTÃO 33
Ionsidere a função recursiva F a seguir,  que em sua execução chama a função G:
1 void F(int n) {
2    if(n > 0) {
3       for(int i = 0; i < n; i++) {
4          G(i);
5       }
6       F(n/2);
7    }
8 }
Iom base nos conceitos de teoria da complexidade,  avalie as afrmações a seguir.
/.     equação de recorrgncia que defne a complexidade da função F é a mesma do algoritmo clAssico 
de ordenação mergesort.
//.   K numero de chamadas recursivas da função F é 4 (log n).
///.   K numero de vezes que a função G da linha 4 é chamada é O(n log n).
 correto o que se afrma em
A  /,  apenas.
B  //,  apenas.
C  / e ///,  apenas.
D  // e ///,  apenas.
E  /,  // e ///.