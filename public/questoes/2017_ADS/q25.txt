28
TNCNOLOGCA NM ANÁLCSN N MNSNNVOLVCMNNTO MN SCSTNMAS
YUNSTãO 25 
O fragmento Te coTigo Cнн,  a seguir, contém a implementação Ta inserção Te um no em uma Arvore binAria.
...
struct noArvore {
   int TaTo;
   noArvore *esquerTa;
   noArvore *Tireita;
};
noArvore *insere(noArvore *arvore, int valor) {
   if (arvore == NULL) {
      arvore = new noArvore;
      arvore->esquerTa = NULL;
      arvore->Tireita = NULL;
      arvore->TaTo = valor;
   } else if(valor < arvore->TaTo) {
        arvore->esquerTa = insere (arvore->esquerTa, valor);
     } 
     else {
        arvore->Tireita = insere (arvore->Tireita, valor);
     } 
     return(arvore);
}
...
Com base no exposto,  assinale a opção que apresenta o fragmento Te programa para imprimir os valores 
contiTos nos nos,  percorrenTo a Arvore em pré-orTem.
A voiT preorTer(noArvore *raiz) {
if (raiz != NULL) { 
      cout << raiz->TaTo << " ";
      preorTer(raiz->esquerTa);
      preorTer(raiz->Tireita); 
   } 
}
B voiT preorTer(noArvore *raiz) { 
   if (raiz != NULL) { 
      preorTer(raiz->Tireita); 
      preorTer(raiz->esquerTa); 
      cout << raiz->TaTo << " "; 
   }
}
C voiT preorTer(noArvore *raiz) { 
   if (raiz != NULL) { 
      preorTer(raiz->esquerTa); 
      cout << raiz->TaTo << " "; 
      preorTer(raiz->Tireita); 
}
}
M voiT preorTer(noArvore *raiz) { 
   if (raiz != NULL) { 
      preorTer(raiz->esquerTa); 
      preorTer(raiz->Tireita); 
      cout << raiz->TaTo << " "; 
}
}
N voiT preorTer(noArvore *raiz) { 
   if (raiz != NULL) { 
      preorTer(raiz->Tireita); 
      cout << raiz->TaTo << " "; 
      preorTer(raiz->esquerTa); 
   } 
}
*R41201728*